# MCP 自动抓取 + 数据存储 设计方案

> 本文所有数字都是**在本机实测**得出的（81 个真实店铺、2026-09-17 的真实数据），不是估算。
> 实测日期：2026-09-19

---

## 0. 结论先行

1. **「点一下自动抓取」做不成纯前端** —— 领星 MCP **不允许浏览器直连**（CORS 实测不支持）。
   必须有中间层。推荐在**本机跑一个小 Node 服务**（不用买服务器、不用装依赖）。
2. **数据量根本不是问题** —— 只要遵守一条规则：**只存"当天有活动"的行**。
   1 年 ≈ 40 万行 ≈ 150 MB。而你现在的担忧（"数据量太大"）来自存全量：
   那是 **41 倍**（1 年 16.7M 行 / 27.8 GB）。
3. **真正的瓶颈是内存，不是磁盘** —— app 现在把全部行读进内存再聚合。
   所以设计重点是**按时间区间查询 + 长周期预聚合**，而不是省存储。

---

## 1. 硬约束：CORS（实测）

我用浏览器会用的方式（带 `Origin: http://localhost:3000`）实测了领星 MCP：

| 测试 | 结果 |
|---|---|
| `OPTIONS` 预检 | **HTTP 400**，无任何 `Access-Control-*` 响应头 |
| `POST`（带 Origin） | HTTP 200，但 **没有 `access-control-allow-origin`** |

→ **浏览器直连会被 CORS 拦截。** 前端无论怎么写都调不通。

### 三个可行方案

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A. 本机 sidecar**（推荐） | 加一个 `npm run hub`，监听 `localhost:8787`。web 的按钮 POST 给它 | key 不进浏览器；不受 CORS 限制；9 分钟的任务不依赖标签页；能落库 | 需要开着这个服务 |
| B. Vite 代理 | 复用 `vite.config.ts` 现有 proxy 模式 | 改动最小 | **只在 dev 生效**，`npm run build` 后不可用 |
| C. 云服务器后端 | 真正部署 | 多人共享、可对外 | 花钱 + 运维 |

**建议先做 A。** A 的代码 95% 能直接搬到 C，不是白做。

> 顺带一提：项目里 `vite.config.ts` 的 `/dashscope`、`/zhipu`、`/deepseek` 三个代理是**同一类问题的现有先例** ——
> 而且是隐患：生产构建后 `import.meta.env.DEV` 为 false，会退回浏览器直连第三方 AI，
> 既可能被 CORS 拦，又把 key 放在前端。这个一起收进 sidecar 就顺带解决了。

---

## 2. 数据量：实测数字

### 2.1 关键前提：只存"有活动的行"

2026-09-17（81 店铺）实测：

| | 行数/天 | 1 年行数 | 1 年原始 | 1 年 gzip |
|---|---|---|---|---|
| 全量 ASIN 目录 | **45,789** | 16,712,985 | **27.8 GB** | 4.6 GB |
| **只存有活动的行** | **1,109** | 404,785 | **674 MB** | 112 MB |

**41 倍差距。** 零活动的 ASIN 对任何汇总（销量/销售额/毛利/广告/流量）贡献都是 **0**，
存了纯粹是负担。这是整个存储方案的地基。

（"有活动"的判定：销量 / 销售额 / 订单量 / 展示 / 点击 / 广告花费 / Sessions / 广告订单量，任一非 0。）

### 2.2 单行 1,747 字节 —— 说明要建表，不是存 JSON

我实测了"只保留 47 列"后的平均单行体积：**1,747 字节**。这比我预期的 450 字节高 4 倍，
原因是 `price_list`、`seller_store_countries`、`parent_asins`、`categories`、`brands`、`principal_names`
这些**嵌套数组被逐行重复**——同一个品名/店铺/品牌在成千上万行里重复存储。

**解法：星型模型（维度表 + 事实表）**

```
dim_shop(sid PK, shop_name, country, country_code, seller_id)      -- 81 行
dim_asin(asin PK, parent_asin, brand, category_l2, product_name, image_url)
dim_msku(msku PK, sid, asin, local_sku, local_name)                -- 可选
fact_daily(sid, asin, date, <45 个数值列>, PK(sid, asin, date))    -- 只有数字
```

事实表去掉所有重复字符串后，单行落到 **~350 字节**：

| | 行数 | 磁盘 |
|---|---|---|
| 1 年 | 404,785 | **~140 MB** |
| 3 年 | 1,214,355 | **~420 MB** |

**磁盘完全不是问题**（你 D 盘还有 262 GB）。而且建了索引之后，按区间查询是毫秒级。

### 2.3 真正的瓶颈：内存

app 现在的做法是把所有行读进内存再聚合（`dataLoader.ts` → `processedData`）。
1 年的 40 万行，作为 JS 对象大约 **400 MB–1 GB** 内存 —— 这才是会卡死的地方。

**所以设计重点是**：
- 前端**只请求当前筛选的时间区间**（默认最近 30 天 ≈ 3.3 万行，内存无压力）
- 长区间（年度、同比）走**预聚合表**，不加载明细

---

## 3. 日期问题：四个必须处理的点

这正是你担心的"日期的问题"。三类数据的"成熟时间"不一样：

### 3.1 数据会回溯修改，不能只拉昨天

| 数据 | 成熟延迟 | 实测证据 |
|---|---|---|
| **流量**（Sessions / PageViews） | **≥ 1 天** | 09-17 的 `sessions` 全为 0（合计 789）；09-16 正常（14,117）。**差 18 倍** |
| 广告 | 通常 T+1 | — |
| 订单 / 退款 / 利润 | 可能回溯修正几天 | 退款是后来才发生的 |

→ 每天只拉 T-1 会得到**永久错误的数字**（而且没人会发现）。

**必须滚动重拉一个窗口**，但全窗口每天都拉太贵：81 店铺 × 13 天 = 1,053 次 × 1.3s ≈ **23 分钟**。

**分层重拉策略**（推荐）：

| 数据年龄 | 重拉频率 | 每天调用 |
|---|---|---|
| T-2 ~ T-4（最近 3 天） | 每天都重拉 | 243 |
| T-5 ~ T-14 | 每周重拉一次 | ~81 |
| T-15 以前 | 不再重拉（已稳定） | 0 |

→ 稳态约 **324 次/天 ≈ 7 分钟**，跑在后台，不占你的时间。
历史首次回补另算（1 年 ≈ 30,000 次调用，按调用的 1.3 秒间隔约 11 小时，建议夜间跑、可断点续）。

**默认抓 T-2**：既然流量有 1 天以上延迟，日常同步默认拉到 T-2，就不会出现"流量列全是 0"。

### 3.2 幂等 upsert —— 没有这条，滚动重拉立刻产生重复

主键 `(sid, asin, date)`，`INSERT ... ON CONFLICT DO UPDATE`。
重拉同一天 = **覆盖**，不是追加。我已用 `node:sqlite` 实测验证（重跑同一天，行数不增加）。

### 3.3 时序字段 vs 快照字段，不能一视同仁

- **时序类**（销量/销售额/毛利/广告/流量/订单）：严格按天，不可重算 → 按天存明细
- **快照类**（`FBA-可售`、评分、评论数）：同一天所有行是"当天状态"
  - 问题：只存有活动的行，会丢掉"零销售但有库存"的 ASIN
  - 解：**库存单独处理** —— 用 `get_fba_stock_list` 或产品表现的库存列，
    只保留**最新快照** + **每周一次历史快照**（做趋势够用），而不是每天 45,789 行

### 3.4 分层保留

| 层 | 范围 | 存什么 | 体积 |
|---|---|---|---|
| 热 | 最近 90 天 | 日粒度明细 | ~10 万行 / 35 MB |
| 温 | 90 天 ~ 2 年 | 周粒度 + 月粒度汇总 | ~1 万行 |
| 冷 | 2 年以上 | 月粒度汇总 | ~1 千行 |

**为什么够用**：app 本来就有**月度/周度两种模式**，温层正好对应；**同比**只需要
"去年同期同区间的汇总值"，周/月粒度完全够；需要日粒度回溯的场景基本都在 90 天内。

---

## 4. 建议架构：本机 sidecar + `node:sqlite`

### 4.1 为什么是本机 Node 服务

一个进程同时解决四件事：

1. **定时抓取** —— 领星 MCP，不受 CORS 限制（Node 直连没问题，已验证）
2. **落库** —— SQLite 单文件，天然支持幂等 upsert 和区间查询
3. **给 web 提供 API** —— 自己加 CORS 头，前端随便调
4. **保管密钥** —— MCP key 留在服务端，**不进浏览器**

**零新增依赖**：Node 24 内置 `node:sqlite`（我已实测可用，含 upsert）。定时用
Windows 任务计划或一个 `setInterval` 就够，不需要 cron 库。

> 也可以选 `better-sqlite3`（性能更好），但内置的已经够用，少一个依赖少一份麻烦。

### 4.2 接口草案

```
GET  /api/health                      → { ok, db, lastSyncAt }
GET  /api/mcp/configs                 → 已配置的 MCP 列表（key 脱敏，只回后 4 位）
POST /api/mcp/configs                 → 新增/更新 MCP
POST /api/mcp/configs/:id/test        → 连通性测试（走 help，不需要数据权限）

POST /api/sync/lingxing               → { dates: [...], force? } 启动抓取，返回 jobId
GET  /api/sync/jobs/:id               → { status, progress, done, failed, log[] }
POST /api/sync/jobs/:id/cancel

GET  /api/rows/asin?from=&to=&shops=&brands=... → 按区间返回明细（分页）
GET  /api/agg/asin?from=&to=&groupBy=week|month → 预聚合
GET  /api/freshness                   → 各指标最后同步到的日期（用于提示"流量待补"）
```

### 4.3 前端怎么改

**关键：不改数据模型**，只加一个"数据来源"选项。

现在：`DataUploadModal` → 解析 Excel → IndexedDB
新增：`DataUploadModal` → 「从领星同步」标签页 → 调 sidecar → 直接把行写进同一个 IndexedDB store

这样**现有的所有分析组件（DashboardOverview / DetailAnalysisModal / 各专项分析）一行都不用改**，
它们仍然从 IndexedDB 读。迁移风险最小。

---

## 5. Web 侧两个新功能

### 5.1 MCP 设置页

- 位置：跟「总控设置」（`AppSettingsButton`）并列，或塞进里面做一个 Tab
- 能力：新增/编辑/删除 MCP；每个有 `name / url / headers / enabled`
- 内置一个「领星」预设（URL 已知）
- 每个 MCP 一个「测试连接」按钮 → 调 `POST /api/mcp/configs/:id/test`
- **key 输入框只回显后 4 位**；保存后前端再也拿不到明文

**用途不止领星**：这个设置页同时给 `ChatBot` 用 —— 让 AI 助手能调用 MCP 工具查数据。
这是 MCP 最本职的用法。

### 5.2 数据上传页的「自动抓取」

```
┌─ 数据上传 ─────────────────────────────┐
│  [手动上传文件]  [从领星同步]   ← 新 Tab │
│                                        │
│  日期范围: [2026-09-01] ~ [2026-09-17] │
│  ☑ 包含流量待补的日期(T-1)              │
│                                        │
│  数据新鲜度:                            │
│   · 09-17 流量未同步 (Sessions=0) ⚠️    │
│   · 09-16 完整                          │
│                                        │
│  [开始同步]                             │
│  ▓▓▓▓▓▓░░░░ 32/81 店铺  剩余约 4 分钟   │
│  日志: 店铺 4346 ✓  4347 ✓  4388 ✓ …   │
└────────────────────────────────────────┘
```

三个必须有的东西：
1. **进度**（81 个店铺要跑 2 分钟以上，没有进度条用户会以为卡死）
2. **可取消**
3. **数据新鲜度提示**（直接告诉用户"09-17 流量没同步"，而不是默默给 0）

### 5.3 数据新鲜度标记

在顶栏（`AppHeader`）显示"数据截至 09-16，09-17 流量待补"。
这条来自我在 `WEB_STRATEGY.md` 里提的"数据范围上下文常驻"，是你现在最需要的一个小改动。

---

## 6. ⚠️ 安全：不要把领星 key 放浏览器

领星 MCP key 的权限范围是**当前账号在 ERP 里的全部数据权限** —— 也就是 81 个店铺的完整读取权。
如果把它存进 localStorage：

- 任何能打开 devtools 的人（你自己、借你电脑的同事、来修电脑的人）都能拿到
- 一旦未来这个页面变成内网共享，等于把整个账号的数据权限公开了

**所以：key 只存服务端**（`D:\...\.env.local` 或 SQLite 的加密字段），
前端只拿"配置是否存在 + 后 4 位"。

同理，`vite.config.ts` 里把 `GEMINI_API_KEY` 通过 `define` 内联进前端包的写法要一起去掉
（我实测过 `dist/assets/*.js` 里**确实**有内联的证据）。

---

## 7. 分阶段

| 阶段 | 内容 | 工期 |
|---|---|---|
| **1** | sidecar 骨架 + `node:sqlite` 建表 + 领星抓取（复用现有脚本逻辑）+ 上传页「自动抓取」Tab + 进度/取消 | 3–4 天 |
| **2** | MCP 设置页（多 MCP 管理 + 测试连接）+ 定时任务（T-2 滚动重拉）+ 数据新鲜度标记 | 2–3 天 |
| **3** | 按区间查询 API + 预聚合，让 app 从"读 IndexedDB"逐步迁到"读服务端" | 1 周 |
| **4** | 分层保留 + 历史回补 + 把 AI 调用也收进 sidecar（顺带修掉 key 内联隐患） | 1 周 |

**阶段 1 结束你就能"点一下，等 2 分钟，数据自己进来"** —— 这是你最想要的那件事。

---

## 8. 需要你定的 3 件事

1. **sidecar 的启动方式**：你希望是
   (a) 手动开一个终端跑 `npm run hub`；
   (b) 装成 Windows 服务/开机自启（要额外配置）；
   (c) 我先做成 (a)，以后再说？

2. **默认同步范围**：日常同步默认拉 **T-2**（避开流量延迟），还是照拉 T-1 但明确标注"流量待补"？

3. **历史回补**：要不要一次性把**今年至今**（约 260 天 × 81 店铺 ≈ 21,000 次调用 ≈ 8 小时）
   补齐？建议夜间跑，跑完你就有完整的趋势和同比了。

---

## 附：这个方案顺手解决的 4 个旧问题

1. **`vite.config.ts` 的 key 内联** —— AI 调用收进 sidecar 后可以彻底删掉 `define`
2. **dev/prod 代理不一致** —— 三个 AI 代理只在 dev 生效的隐患一并消除
3. **数据锁在浏览器里** —— 从 IndexedDB 升级为 SQLite，换电脑/清缓存不再丢
4. **纯前端撑不起自动同步** —— 这正是 `WEB_STRATEGY.md` 里"服务端数据落地"那条的具体落地
