# 亚马逊业绩报告 Web —— 产品战略方案

> 视角：跨境业务线负责人（P&L owner），不是外包顾问。
> 定位：**本文件谈「做什么、为什么、先做什么」，不谈工程清理细节** —— 那些已经在
> `APP_OPTIMIZATION_PLAN.md` 里排得非常清楚了，我不重复。
> 编写依据：我实际读过并改过这个仓库的代码（修暗色模式、加子表显示开关），
> 文中所有数字与文件名都是我在代码里核对过的，不是估计。

---

## 0. 一句话结论

**别再把它当「报表工具」做，把它当「利润漏损的早期预警系统」做。**

现在的产品能把数据变成**好看的图表**，但还没能把图表变成**当天必须处理的几件事**。
功能已经够多了（P&L、广告、库存、退货、评论、关键词、利润试算、异常雷达、AI 顾问），
真正缺的是三样东西：**数据自动进来**、**口径只有一份**、**问题主动找人**。

如果只能记住一句话：

> 跨境团队缺的从来不是数据，是**决策带宽**。一个人管 500 个 SKU，
> 他不可能逐个看。所以产品的终局是「问题找人」，不是「人找问题」。

---

## 1. 我先把定位说清楚

### 1.1 现在它是什么

一个纯前端的、单机的、Excel 驱动的分析看板。用户手动从亚马逊后台下载 8 类报表，
手动上传，前端解析，算出指标，展示，然后用户复制到飞书 / 导出 Excel。

### 1.2 它应该是什么

| 维度 | 现在 | 应该 |
|---|---|---|
| 数据来源 | 人工下载 + 人工上传 | 服务端自动同步（SP-API / Ads API） |
| 触发方式 | 用户主动点开 | 异常主动推送（飞书 / 企微） |
| 输出物 | 图表 + 可复制的表格 | 带金额、责任人、截止时间的**动作** |
| 口径 | 散落在各处的公式 | 一份声明式定义，全局唯一 |
| 价值锚点 | 「我今天看清楚了」 | 「我今天因此改了一件事」 |

### 1.3 为什么这个定位值钱

跨境业务的毛利漏损主要来自四个口子：**广告浪费、库存滞销/断货、退款异常、定价失误**。
这四件事的共同点是——**它们都是可以提前 N 天被发现的**。

所以这个工具的价值公式很简单：

```
工具价值 = 提前发现天数 × 单日漏损金额 × 命中率
```

一个年销 5000 万美金的盘子，哪怕只挽回 0.5% 的毛利，也是 25 万美金级别。
而它的开发成本是**人月**级别。这个 ROI 不需要论证，需要的是**别把它做成一个玩具**。

---

## 2. 现状体检（我在代码里核对过的）

### 2.1 产品资产（这些是真优势，别丢）

1. **本地优先**：主数据在浏览器解析和存储，敏感经营数据不出本机。对内部数据这是优势，不是缺陷。
2. **指标覆盖完整且经营导向正确**：以**毛利额**为顶层目标，而不是炫流量。`BusinessCommandCenter` 的异常雷达按毛利影响排序——**方向是对的**。
3. **动作雏形已经存在**：异常可以生成负责人、截止时间、状态，且持久化在 IndexedDB。**这是全项目最值钱的资产，但现在只是个雏形。**
4. **数据缺失意识好**：区分「真实为 0」和「对比期无数据」，并有完整性告警。很多商业 BI 都没做到。
5. **下钻链路已通**：看板 → 子表 → 专项分析（退货/评论/关键词）→ 利润试算，路径是完整的。

### 2.2 我实测出来的三个结构性问题

**问题一：数据可信链的根基是「人工纪律」**

现在整个产品建立在「用户每天记得下载 8 类报表、记得上传、记得传对」之上。
只要漏传一个文件，看板是错的，**而看板不会告诉用户它是错的**（虽然有告警，但不是强阻断）。

这不是 UI 问题，是产品形态问题。**一半的「数据不可信」抱怨，根源在这里。**

**问题二：口径没有单一来源（这条我最不能忍）**

我在代码里数过：

| 现象 | 实测 |
|---|---|
| `safeRatio(...)` 占比计算 | **25 处** |
| `gross_margin` 相关计算/引用 | **41 处** |
| 「广告占比」这个名字 | **7 个文件里出现 15 次** |
| 同一个公式 `safeRatio(d.ad_spend, d.sales_amount)` | 在 `DetailAnalysisModal.tsx` 里**字面重复写了 2 遍**（月/周两套列配置） |

后果是**口径漂移**：同一个「毛利率」在主表、P&L 子表、流量子表、导出 Excel、
AI 提示词里，可能是五个不同的算法。对财务口径来说这是致命的——
**运营和财务对不上账的那一天，这个工具就死了。**

**问题三：依赖 CDN，而且是生产环境**

我核对过 `dist/index.html`（也就是真正部署的产物）——它**仍然**在加载：

- `https://cdn.tailwindcss.com` ← Tailwind 官方明确说 Play CDN 不能用于生产
- `@import url('https://fonts.googleapis.com/...')`
- 一套指向 `esm.sh` 的 import map

这意味着：**Tailwind CDN 挂掉 = 整个应用变成无样式白页**。
而且这个团队是跨境业务，**Google Fonts 在中国大陆是访问不了的**。

> 我修暗色模式时亲身踩到了这个坑：为了让覆盖规则压过 Tailwind 运行时注入的样式，
> 我被迫给 **200 多条规则**全部加 `!important`。这就是 CDN 方案在持续制造的技术债——
> 它不只是性能问题，它让每一处样式改动都变得脆弱。换成构建期 Tailwind 之后，
> 这些 `!important` 可以整体删掉。

### 2.3 密钥：现在没泄漏，但**已经上膛**

`vite.config.ts` 里有：

```js
define: {
  'process.env.API_KEY': JSON.stringify(env.GEMINI_API_KEY),
}
```

我验证了它的实际效果：`dist/assets/index-DgpThuqq.js` 里**确实**存在
`PLACEHOLDER_API_KEY` 这个字符串——说明 `define` 会把 `.env.local` 的值**原样内联进前端包**。

现在的 `.env.local` 里是占位值，所以**还没有真实泄漏**。但只要有人把真 key 填进去
再跑一次 `npm run build`，这个 key 就会发给每一个访问者。

**这把枪现在是上膛状态。** 这是我认为唯一需要「今天就处理」的安全项。

### 2.4 规模与可维护性

- 40 个前端源文件，**15,575 行**
- `DetailAnalysisModal.tsx` **159.5 KB / 约 2,778 行**——一个文件等于一个中型项目
- `RefundAnalysisModal` 79 KB、`ReviewAnalysisModal` 78 KB，**各自独立实现了一遍 SVG 图表、导出、AI 提示词**

三个弹窗里三套图表代码，意味着改一次配色要改三个地方——**这也是我修暗色模式时最费劲的原因**。

---

## 3. 对现有 `APP_OPTIMIZATION_PLAN.md` 的态度

**我同意绝大部分**，尤其是它的优先级判断和「暂不建议立即做的事项」。那份计划写得比我见过的多数外包方案都好。具体地：

- ✅ 同意「数据不丢与指标正确优先于一切」
- ✅ 同意「先不要全面品牌视觉翻新」
- ✅ 同意「不要为手机端复刻宽表」
- ✅ 同意「P0 完成前不加新大模块」

**我要挑战一条**，并且这条很关键：

> 原文：「过早引入账号、权限和多人后台；先验证单机工作流的真实使用频率和闭环价值。」

我同意**不要做账号权限系统**——那是重资产、低回报。
但我强烈反对把它和「**服务端数据落地**」混为一谈，这是两件事：

| | 账号/权限/多人后台 | 服务端数据落地 |
|---|---|---|
| 成本 | 高（认证、RBAC、审计、迁移） | 低（一个对象存储 + 一个定时任务） |
| 回报 | 中（协作、管控） | **极高**（历史数据可积累、跨期分析、口径统一、换电脑不丢） |
| 是否该做 | 暂缓，同意 | **应该现在做** |

**理由**：纯前端意味着**所有数据资产都锁在每个人的浏览器里**。
换个电脑、清一次缓存，历史就没了。没有历史数据，就永远做不了真正的
「趋势预警」和「同比归因」——而那恰恰是这个产品最值钱的能力。

而且：**没有服务端，数据接入自动化就无从谈起**。而数据接入决定产品天花板（见 4.1）。

---

## 4. 战略主线：三件事，按顺序做

### 4.1 第一件：让数据自动进来（决定天花板）

这是**最重要的单点决策**。现在「人工下载 + 人工上传」的形态，直接决定了：

- 数据新鲜度上限 = 用户的勤快程度
- 用户规模上限 = 愿意每天花 20 分钟传报表的人
- 可信度上限 = 人工不出错

**终局**：Amazon SP-API（报告 API / 订单 / 库存 / 退货）+ Ads API 自动同步，服务端定时拉取。

**但不要一步跳到终局。** 我建议分三档，价值递增、风险可控：

| 档位 | 做法 | 新增成本 | 解决什么 |
|---|---|---|---|
| **A（立刻）** | 保留上传，但**上传即强校验**：缺哪个槽位、哪个文件解析失败，直接挡住并说清楚 | 小 | 消除「静默的错数据」 |
| **B（1 个季度）** | 服务端定时拉取（SP-API 报告），页面显示「数据截至 X，自动同步于 Y」 | 中 | 消除人工纪律依赖 |
| **C（2-3 个季度）** | 订单/库存实时增量同步，支持任意时间粒度查询 | 大 | 打开跨期分析、实时预警 |

**我的决定：A 现在做，B 下个季度做，C 等 B 验证后再定。**

理由：A 是纯收益，B 是形态跃迁，C 在 B 没跑通之前是赌博。

### 4.2 第二件：建立指标语义层（决定可信度）

**这是我最想要的一件技术资产，也是我作为业务负责人会亲自盯的一件事。**

要做的事很简单，但影响很大：**把指标定义收敛成一份声明式清单。**

```ts
// metrics/adRatio.ts —— 全局唯一来源
export const adRatio = defineMetric({
  id: 'ad_ratio',
  label: '广告占比',
  formula: (d) => safeRatio(d.ad_spend, d.sales_amount),
  format: 'percent',
  // 口径说明：广告花费 / 销售额，按【聚合后的行】计算，不是各行占比的平均
  definition: '广告花费 ÷ 销售额（行级聚合口径）',
  owner: '财务口径委员会',
});
```

然后：**前端列配置、导出 Excel、AI 提示词的数据块、异常规则——全部从这个注册表生成。**

收益：

1. **口径不可能漂移**（改一处，全局生效）
2. **AI 才可能可信**——现在 AI 看到的是各处拼凑的数字；统一后它看到的和财务看到的是同一套数
3. **新人能读懂口径**（`definition` 字段直接变成产品内的「口径说明」tooltip）
4. **这是唯一能让「指标对账测试」自动化的前提**

为什么我把它排在数据接入之后、而不是之前？因为**没有稳定的数据源，统一口径只是在给流沙浇水泥**。
但两件事可以并行推进，语义层不依赖服务端。

**具体第一步**（很小，但立刻有价值）：先把「广告占比」这一个指标收口，验证模式跑得通，再批量迁移。不要一次性重构 41 处。

### 4.3 第三件：让问题主动找人（决定使用频率）

**产品的成败在这一条上。**

现在的模式：用户**主动打开** → 看 → 自己判断 → 自己决定做什么。
问一句残酷的话：**用户凭什么每天打开它？**

答案不能是「因为报表好看」，只能是「**因为不打开会出事**」。

具体做法：

1. **把异常雷达产品化**。代码里 `BusinessCommandCenter` 已经有异常 + 负责人 + 截止时间的雏形，但它是个「要用户点开才能看到」的看板。
2. **每日早晨一份简报，推到飞书 / 企微**：昨天谁掉了、掉了多少毛利、最可能的原因、建议动作。**带一键认领。**
3. **建立闭环**：动作 → 截止时间 → 数据更新后自动回填效果（"你 3 天前处理的那个 ASIN，毛利恢复了 X"）。

第 3 点是决定性的。`APP_OPTIMIZATION_PLAN.md` 里 P1-03 也提到了，我完全同意并愿意把它提到 P1 第一位。
**因为「有效果回填」是唯一能让用户相信「这个工具帮我赚了钱」的机制** —— 也是续费的唯一理由。

---

## 5. 目标架构

```
┌─────────────────────────────────────────────────────────┐
│  触达层：飞书/企微机器人 · Web 看板 · 每日简报           │  ← 问题找人
├─────────────────────────────────────────────────────────┤
│  动作层：动作清单 · 责任人 · 截止时间 · 效果回填         │  ← 闭环（已有雏形）
├─────────────────────────────────────────────────────────┤
│  判断层：异常规则引擎 · AI 归因（输入必须是语义层）      │
├─────────────────────────────────────────────────────────┤
│  指标语义层：声明式指标注册表（单一来源）                │  ← 新增，最关键
├─────────────────────────────────────────────────────────┤
│  数据层：SP-API/Ads 同步 → 标准化 → 服务端存储 + 本地缓存 │  ← 新增
├─────────────────────────────────────────────────────────┤
│  接入兜底：Excel 上传（强校验 + 槽位完整性）             │
└─────────────────────────────────────────────────────────┘
```

技术上的关键判断：

- **指标语义层**是纯前端就能做的，先做，收益立刻兑现。
- **服务端**只做三件事：拉数据、存数据、发通知。**不做账号、不做权限、不做多人后台。**
- **AI 调用必须走后端**（同时解决密钥泄漏、成本控制、数据出境合规三个问题）。
- **Tailwind 换构建期**，顺带删掉我加的那 200 多个 `!important`。

---

## 6. 路线图

### 第 0 阶段：止血（现在 —— 1 周）

**冻结新功能。** 理由：现在每加一个功能，都是在不可信的数据底座上再加一层杠杆。

1. **移除前端密钥注入**（改 `vite.config.ts` 的 `define`），AI 调用改服务端代理或至少改为用户自己的 key 只在浏览器内使用、绝不打进包。（**今天就做**）
2. 移除 Tailwind CDN + Google Fonts，改构建期。（顺带删掉 200+ 个 `!important`）
3. 加 CI：现有 `verify-dark-theme` / `verify-subtable-display` 两个校验脚本进流水线，加 `tsc --noEmit`。
4. 上传强校验：缺槽位 / 解析失败直接挡住。

> 这 4 条做完，产品从「可能出错」变成「不会静默出错」。这是所有后续工作的前提。

### 第 1 阶段：可信 + 闭环（1 —— 2 个月）

5. **指标语义层**：先收口「广告占比」1 个指标验证模式，再迁移 Top 10 指标。
6. **效果回填闭环**：动作完成 → 数据更新 → 自动展示前后对比。
7. **每日简报推送**（飞书 / 企微），带一键认领。
8. **服务端数据落地**（轻量版）：对象存储 + 定时任务 + 历史数据可查。**不做账号。**
9. 数据范围上下文常驻（周期、数据截至日、生效筛选）——同意现有计划的 P1-01。

### 第 2 阶段：自动化（3 —— 6 个月）

10. **SP-API 自动同步**（报告 API 先行），页面上明确标注数据新鲜度。
11. **AI 从「聊天框」变成「例行巡检」**：每天早上自动跑一遍经营诊断，输出可追溯的证据链（每个结论必须能点回到具体数字）。
12. **拆 God component**：把表格引擎、图表、导出抽成通用能力。现在三个大弹窗各一套，是最大的维护负担。
13. 虚拟滚动 + 按需加载（现有计划 P2-01/P2-02）。

### 第 3 阶段：规模化（6 个月 +）

14. 库存 - 广告 - 利润的**联动规则引擎**（现在只在 AI 提示词里有一条 Rule Zero，没产品化）。
15. 归因下钻：毛利掉了 → 一键拆到「哪个 ASIN / 哪个词 / 哪个费用项」。
16. 跨期趋势预警：基于积累的历史数据做真正的「同比归因」。

---

## 7. 明确不做的事（战略克制）

这一节和路线图一样重要。**一个负责人的价值，一半体现在他说「不做什么」。**

| 不做 | 理由 |
|---|---|
| ❌ 支持 Walmart / TikTok / Shopee 等全平台 | 先把 Amazon 做到「离了它不能干活」。多平台 = 多套口径 = 全部都很浅 |
| ❌ 手机端复刻宽表 | 同意现有计划。移动端只做**通知和认领**，不做分析 |
| ❌ 自定义报表设计器 / BI 平台 | 那是 Tableau 的活。我们做的是「判断」，不是「画图工具」 |
| ❌ ERP / 进销存 / 财务记账 | 越界。只做利润与效率的判断，不做出入库管理 |
| ❌ 账号权限 / 多人后台 | 现阶段重资产低回报（同意现有计划）。但**服务端数据落地要做**，见第 3 节 |
| ❌ 在没有成本预算和效果评估前扩大 AI 场景 | AI 是手段不是卖点。每一处 AI 都要能回答「它比规则引擎强在哪」 |
| ❌ 追求视觉翻新 | 现有 UI 够用。信息密度比好看重要 |

---

## 8. 组织与度量

### 8.1 团队配置（别更大）

| 角色 | 人数 | 职责 |
|---|---|---|
| 业务负责人（你） | 1 | 口径定义、优先级、验收。**指标语义层的 owner** |
| 前端 | 2 | 主体开发、组件收敛 |
| 后端 | 1 | 数据同步、通知、AI 代理 |
| 数据 | 0.5 | SP-API 字段映射、口径对账 |
| 设计 | 0.5 | 只在信息架构层面介入，不做视觉翻新 |

总共 5 人。**超过这个规模，沟通成本会超过产出。**

### 8.2 北极星指标

我不用「DAU」「页面停留时长」这类虚荣指标，因为这个工具的价值不在于「用了多久」，
而在于「因此改了什么」。

> **北极星：每周被真实采纳并回填效果的动作数。**

配套指标：

| 维度 | 指标 | 目标 |
|---|---|---|
| 决策闭环 | 每周采纳动作数 | 持续增长 |
| 决策闭环 | 动作效果回填率 | ≥ 80% |
| 可信度 | 财务口径对账差异 | 0 |
| 效率 | 从数据更新到发现 Top3 问题的中位时长 | ≤ 3 分钟 |
| 数据新鲜度 | 数据截至日与今天的差距 | ≤ 1 天 |
| 稳定性 | 静默错数据发生次数 | 0 |

（后三项与现有计划的指标一致，说明那份计划的度量选得对。）

### 8.3 节奏

- **2 周一个 sprint**
- **每个 sprint 必须有一个「运营愿意每天用」的产出**——如果做不到，说明这个 sprint 排错了
- 每个季度做一次**口径审计**：随机抽 10 个指标，拿财务数据对账

---

## 9. 如果我只有一个月

按这个顺序，做完就停：

1. **第 1 天**：改掉 `vite.config.ts` 的密钥注入。这件事零成本、零风险，但不做就是定时炸弹。
2. **第 1 周**：Tailwind 换构建期 + 去掉外部 CDN。让产品在弱网/内网/断网时不会变成白页，同时删掉 200+ 个 `!important`。
3. **第 2 周**：上传强校验 + CI（`tsc` + 两个校验脚本）。让错误不再静默。
4. **第 3 周**：指标语义层，只收口「广告占比」，把模式验证出来。
5. **第 4 周**：把异常雷达 + 动作清单接上飞书推送，跑通「问题找人」的第一次闭环。

一个月后，你会得到一个**不会静默出错、断网能用、有口径依据、且会主动找你**的产品。
这比再增加三个分析模块有价值得多。

---

## 10. 风险清单

| 风险 | 影响 | 应对 |
|---|---|---|
| SP-API 申请周期长 / 被拒 | 高：卡住第 2 阶段 | **现在就开始申请**，不要等排期。同时 A 档（强校验）先顶上 |
| 用户其实不需要「自动同步」 | 高：方向性错误 | 先小范围灰度，用「你愿意每天花 20 分钟传报表吗」直接问 |
| 口径统一触动了某些人的利益 | 中 | 口径由**财务**最终裁定，不是由产品决定。这是组织问题不是技术问题 |
| AI 成本失控 | 中 | 服务端强制预算 + 缓存 + 降级；每次调用记录成本 |
| 纯前端数据丢失 | 中高 | 服务端落地之前，至少做**导出全量备份**和**恢复引导** |
| 团队被新功能需求带偏 | 高 | 用第 7 节「不做清单」当挡箭牌。**新功能一律进 backlog，不进 sprint** |

---

## 附：这个方案的三个核心判断

1. **数据接入决定天花板**——人工上传的形态，撑不起「每天必用」的产品。
2. **指标语义层决定可信度**——口径漂移是这类工具最常见的死法，而且死得很安静。
3. **问题找人决定使用频率**——没有人会为了「看报表」每天打开一个网站。

三条按顺序做，做完这个产品就是一个**系统**，而不是一个**看板**。
