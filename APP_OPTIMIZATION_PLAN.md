# 亚马逊业绩报告 App 优化计划

> 审查日期：2026-08-30  
> 审查范围：产品定位、核心任务流、信息架构、UI/响应式、数据可信度、AI 安全、性能、可访问性、工程质量  
> 本文性质：优化路线图，不包含本轮代码改动

## 1. 结论先行

当前产品已经越过“原型”阶段，具备月/周业绩、目标、广告、库存、退货、评论、关键词、利润试算、经营异常和 AI 顾问等较完整的业务能力。真正的短板不是功能数量，而是以下四条交付链还不够可靠：

1. **数据可信链**：导入开始时先清空旧库，后续任意文件解析失败都可能留下空库或半成品；持久化错误被静默吞掉。
2. **决策闭环链**：看板能发现问题，也能创建动作，但“问题金额—负责人—截止时间—处理结果—更新后效果”还没有形成完整闭环。
3. **生产交付链**：TypeScript 校验未通过、没有测试/CI、首包过大，并依赖 Tailwind CDN 和外部字体运行。
4. **体验一致性链**：固定侧栏、基于视口的断点和大量独立弹窗导致中等宽度、键盘操作和复杂任务切换体验不稳定。

建议把下一版本目标定义为：

> **让运营人员在导入数据后 3 分钟内，可信地找到影响毛利最大的 3 个问题，并生成可追踪的负责人动作。**

在 P0 完成前，不建议继续增加新的大模块。

## 2. 本次审查依据

### 2.1 已验证事实

- 独立目录生产构建成功，但主业务包约 **2,218 KB / gzip 643 KB**，ExcelJS 包约 **940 KB / gzip 271 KB**，构建已触发大包警告。
- 默认 `dist` 构建在清理旧产物时遇到权限锁；独立输出目录可正常构建。
- `npx tsc --noEmit` 未通过：`dataLoader.ts:419` 的图片 `imageId` 存在 `string` 到 `number` 的不安全类型断言。
- 源码中没有测试文件；`package.json` 也没有 `test`、`lint`、`typecheck` 脚本。
- `DetailAnalysisModal.tsx` 约 2,776 行，另有 5 个核心文件超过 800 行；主要弹窗和重型依赖均由首屏同步加载。
- 全项目约有 19 处全屏弹层，只有 1 处声明 `role="dialog"` / `aria-modal`，未发现焦点圈定逻辑。
- 主布局侧栏固定为 288px；月度/周度开关在 `<1280px` 时直接隐藏。
- 经营异常三栏在 `lg` 视口即启用，但三栏最小宽度合计约 900px；叠加固定侧栏后，在 1024px 等常见宽度下有溢出风险。
- `index.html` 使用 Tailwind CDN、Google Fonts 和一套额外 import map；正式产物仍依赖外网资源。
- AI Key 保存在 `localStorage`，Gemini Key 还支持在构建期注入客户端包；AI 请求会把经营上下文直接发往第三方模型。

### 2.2 本轮未完成的验证

- 本会话未获得可用的浏览器自动化接口，因此没有把视觉回归截图当成已验证证据。
- 仓库没有脱敏的标准样例数据，尚未对业务公式做财务对账和大文件压力测试。
- 以上两项应作为 Phase 0 的第一批基线任务，而不是被省略。

## 3. 做得好的部分

- **业务覆盖完整**：P&L、广告、库存、退货、评论、关键词和利润试算之间已有下钻关系。
- **本地优先**：主数据在浏览器本地解析和存储，适合内部敏感经营数据场景。
- **数据缺失意识较好**：区分“真实为 0”和“对比期无数据”，并已有完整性告警。
- **经营导向正确**：异常雷达以毛利额为核心目标，而非只展示流量或销售额。
- **动作雏形已存在**：异常可生成负责人、截止时间和状态，并保留在 IndexedDB。
- **细节能力丰富**：筛选快照、趋势、复制到飞书、深色模式等提高了高频运营效率。

后续优化应保留这些优势，重点做收敛、可信和提速。

## 4. 优先级总表

| ID | 优先级 | 问题/机会 | 业务影响 | 实施量 | 核心验收 |
|---|---|---|---|---|---|
| P0-01 | P0 | 导入非事务化，先删旧数据再解析 | 极高：失败可造成数据丢失/半成品 | M | 任一文件失败时旧数据和当前筛选 100% 不变 |
| P0-02 | P0 | DB 写入错误被吞掉，且多数写入未等待完成 | 极高：页面显示成功但刷新后丢失 | M | 提交完成前等待全部事务；失败可见、可重试、可回滚 |
| P0-03 | P0 | AI Key 与经营数据直接暴露给浏览器/第三方 | 极高：密钥、合规、数据出境风险 | L | 不在包内注入生产 Key；首次发送前展示范围并取得确认 |
| P0-04 | P0 | 类型检查失败且无测试/CI | 高：指标回归无法及时发现 | M | `build + typecheck + unit + smoke` 全绿后方可发布 |
| P0-05 | P0 | 1024–1280 宽度下关键开关隐藏、复杂模块溢出 | 高：常见办公屏核心功能不可用 | M | 1024/1280/1366/1440/1920 全流程无功能丢失 |
| P0-06 | P0 | 生产依赖 Tailwind CDN/外部字体 | 高：内网、弱网或 CSP 下界面失效 | S | 断网环境仍可完整渲染；移除运行时 CDN/import map |
| P1-01 | P1 | 首屏缺少“数据范围、刷新时间、有效筛选”可信上下文 | 高 | S | 任意页面 5 秒内能确认当前口径与数据新鲜度 |
| P1-02 | P1 | 异常按严重度排序，未以财务影响和置信度排序 | 高 | M | 默认展示预计毛利影响、证据完整度、优先级原因 |
| P1-03 | P1 | 动作缺少更新后效果回填 | 高 | M | 数据更新后自动提示复盘并展示前后指标 |
| P1-04 | P1 | 空状态只有“去导入”，缺模板、样例和校验预期 | 中高 | S | 新用户可凭模板在 5 分钟内完成首次分析 |
| P1-05 | P1 | 弹窗体系不统一、键盘和焦点管理缺失 | 中高 | M | 全部弹窗满足 Esc、焦点圈定、焦点返回、语义标签 |
| P1-06 | P1 | 页面纵向堆叠过长，跨模块切换成本高 | 中 | M | 提供模块锚点/页签，当前位置和筛选上下文持续可见 |
| P2-01 | P2 | 重型模块全部首屏加载，主包过大 | 中高 | M | 初始业务 JS gzip < 300 KB；Excel/导出/AI 按需加载 |
| P2-02 | P2 | Excel 解析运行在主线程，无大小限制/取消/阶段进度 | 高（大数据） | L | 50 MB 目标样本解析时 UI 可交互，有进度、取消和错误行报告 |
| P2-03 | P2 | 超大组件与重复计算/导出逻辑难维护 | 中高 | L | 核心模块按领域拆分；业务公式拥有单一来源与测试 |
| P2-04 | P2 | 缺错误边界、埋点与性能基线 | 中 | M | 错误可恢复；关键漏斗和耗时可观测且不采集经营明细 |

实施量：S = 0.5–2 人日，M = 3–7 人日，L = 8 人日以上；正式排期需结合样例数据和目标部署方式校准。

## 5. 分阶段路线图

### Phase 0：建立基线（2–3 天）

目标：先把“什么算正确、什么算快、什么屏幕必须可用”定义清楚。

#### 任务

- 准备 3 套脱敏金标数据：正常、字段缺失/别名混乱、10–50 MB 大文件。
- 为 20 个核心指标建立手工核对表：销售额、毛利额、毛利率、广告花费、ACoS、退款率、库存成本、库龄占比、目标序时等。
- 记录当前任务基线：首次导入时长、再次打开时长、筛选响应时间、打开子表时间、AI 请求前的数据范围。
- 补充视口基线：1920×1080、1440×900、1366×768、1280×800、1024×768；手机只保证关键摘要和导入提示，不承诺完整宽表操作。
- 把 `build`、`typecheck`、最小 smoke test 接入本地和 CI。

#### 验收

- 金标数据和期望结果进入版本库，且不包含真实客户/店铺敏感信息。
- 现有 TypeScript 错误被登记并修复。
- 每个后续优化项都能用基线数据或行为指标验证，不以“看起来更好”验收。

### Phase 1：可信数据与安全底座（1–2 周，必须先做）

#### 5.1 重构导入为“解析—校验—预览—提交”

建议流程：

1. 在内存或临时 IndexedDB 区解析全部文件。
2. 校验文件类型、必要列、日期覆盖、重复行、金额/比例范围和跨表关联率。
3. 展示导入摘要：新增/覆盖行数、有效日期、未识别列、缺失表、异常样例。
4. 用户确认后，以单个 IndexedDB 事务写入正式 stores。
5. 全部写入成功后再切换 UI 状态；失败则完整保留旧数据。
6. 保留最近一次可用快照，并提供“撤销本次导入”。

额外要求：

- `saveToDB` 不再吞掉异常；统一返回成功/失败结果。
- 所有写入必须 `await`，提交按钮显示明确阶段和耗时。
- 搜索词、规则版本、筛选快照是否随主数据更新，需要明确展示而非隐式清空。
- 周度模式切换默认选择“导入数据中的最近完整周”，而不是系统当前时间的上周。

#### 5.2 AI 安全与数据出境控制

- 删除生产环境构建期注入客户端 API Key 的方案。
- 企业部署优先采用同域 BFF/代理：服务端保管 Key、限制模型与额度、记录非敏感审计信息。
- 如果保留“用户自带 Key”，在界面明确说明存储位置、风险和清除方式，默认不长期保存。
- AI 首次使用前展示“将发送的数据范围”，支持去掉 ASIN、店铺、负责人、评论原文等字段。
- 提供“仅发送聚合数据”默认模式；敏感明细模式需二次确认。
- 增加超时、取消、重试、限流、错误分类和模型不可用回退。

#### 5.3 工程质量门禁

- 增加 `lint`、`typecheck`、`test`、`test:e2e` 脚本。
- 逐步启用 `strict`、`noImplicitAny`、`noUncheckedIndexedAccess`；先覆盖数据解析与财务计算。
- 对日期、货币、分母为 0、缺失对比期、跨月周、时区、负数退款等建立单元测试。
- 引入错误边界，页面级错误提供“重试/恢复旧数据/导出诊断信息”。
- 发布流程使用全新产物目录和原子替换，避免现有 `dist` 锁导致发布不确定。

#### Phase 1 发布门槛

- 导入任一步骤失败时，旧数据、旧筛选、旧动作均不变。
- 金标数据核心指标差异为 0。
- 客户端包和源码中不存在生产 Key。
- CI 中构建、类型检查、核心单元测试和导入 smoke test 全部通过。

### Phase 2：决策效率与信息架构（1–2 周）

#### 5.4 重做首屏层级

首屏应按“上下文—结果—异常—动作”组织：

1. **上下文条**：当前周期、数据截至日期、覆盖率、有效筛选、月/周模式、最后更新时间。
2. **核心结果卡**：毛利额为主卡；销售额、毛利率、广告花费和库存风险为支撑卡。
3. **预测与差距**：按当前速度预测月末结果，同时显示“距目标还差多少金额/每天需要多少”。
4. **Top 3 经营问题**：默认按预计毛利影响排序，而非只按颜色/严重度。
5. **动作区**：逾期、处理中、待复盘动作；详细诊断默认折叠。

UI 建议：

- 把世界时钟降为可选小组件，首屏空间优先给数据新鲜度与目标差距。
- 顶栏补充产品名称或当前视图名称，避免只有图标。
- 有效筛选用可移除 chips 展示，提供“一键清除”和“保存视图”。
- 正常状态也要有积极反馈，例如“本期未发现高优先级异常”，不要只在异常时占据大面积。

#### 5.5 异常雷达升级为可解释的优先队列

每条异常统一显示：

- 预计毛利影响金额/区间。
- 规则版本与阈值。
- 数据证据、数据覆盖率和置信度。
- 推荐负责人、截止时间、预计收益。
- 下一步动作和可直接下钻的专题。

排序建议：`预计毛利影响 × 置信度 × 紧急系数`。严重度继续保留，但不作为唯一排序依据。

#### 5.6 完成动作闭环

- 数据更新后，自动识别“待复盘动作”。
- 展示动作创建前后的关键指标、变化幅度和是否达到预期。
- 允许填写结论、原因、附件/链接和下一步。
- 增加“已完成但无改善”“继续观察”“规则误报”等结果类型，为后续规则优化提供反馈。
- P2 再考虑飞书任务/消息协同；P1 先把单机闭环做正确。

#### 5.7 优化首次使用和空状态

- 空状态直接提供三个 CTA：下载模板、加载脱敏样例、导入自己的数据。
- 模板内提供字段说明、示例值、必填/选填、货币/百分比口径。
- 导入区支持拖拽、批量识别、文件大小提示和“为什么未识别”。
- 解析报告从技术日志改成业务语言，并允许导出错误行。

### Phase 3：UI 系统、响应式与可访问性（1–2 周）

#### 5.8 响应式策略

产品以桌面为主，优先保证 1024–1920px：

- `<1280px` 时侧栏改为可折叠抽屉，不再简单隐藏月/周切换。
- 用容器查询或内容区宽度决定经营雷达列数，避免仅用视口 `lg/xl`。
- 1024/1280 下核心卡为 2 列，雷达为 1 列；1366/1440 根据内容区采用 2 列；宽屏再采用 3–5 列。
- 宽表继续横向滚动，但冻结维度列、指标表头和总计，并提供列组显隐。
- 顶栏在窄屏保留周期、模式和导入，世界时钟/次要设置进入更多菜单。

#### 5.9 统一设计系统

- 把颜色、圆角、阴影、间距、字体、状态色整理为 CSS variables/design tokens。
- 移除通过大量 `!important` 覆盖 Tailwind 类的暗色模式，改为语义 token。
- 统一卡片密度；给宽表提供“紧凑/舒适”密度切换。
- 统一成功、警告、错误、处理中状态，不只依赖红绿颜色。
- 原生 `alert/confirm` 统一替换为 toast 和确认弹窗，提供可恢复操作。

#### 5.10 统一弹窗与键盘行为

建立共享 `Dialog` 基础组件：

- `role="dialog"`、`aria-modal="true"`、标题/描述关联。
- 打开时聚焦首个有意义控件，Tab 焦点不逃逸，关闭后回到触发按钮。
- Esc 只关闭最上层；危险操作不可因误触遮罩直接关闭。
- 锁定背景滚动，支持多层弹窗的层级管理。
- 图标按钮有可读名称，表格排序/筛选状态可被屏幕阅读器识别。
- 将 `html lang` 改为 `zh-CN`，并检查 200% 缩放和高对比度。

### Phase 4：性能、可维护性与规模化（1–2 周）

#### 5.11 首屏拆包

- 使用 `React.lazy` 按需加载退货、评论、关键词、利润试算、趋势、AI 设置等模块。
- ExcelJS、SheetJS、html2canvas、jsPDF 只在导入/导出时加载。
- 移除 Tailwind CDN、Google Fonts 外链和无效 import map；字体自托管或使用系统字体栈。
- 设置 bundle budget，并在 CI 超标时失败。

目标：

- 初始业务 JS gzip `< 300 KB`，重型导入/导出模块不进入首包。
- 目标办公设备冷启动到可操作 `< 2.5s`。
- 已缓存数据恢复到可操作 `< 1.5s`。

#### 5.12 大文件处理

- Excel 解析迁移到 Web Worker，主线程只负责进度和结果展示。
- 设置建议大小阈值和硬上限；超限时说明如何拆分。
- 阶段进度至少包含：读取、识别表头、解析、校验、关联、提交。
- 支持取消；取消或崩溃不写入正式数据库。
- 对大型子表采用成熟的行/列虚拟化，避免一次创建过多 DOM。

#### 5.13 按领域拆分代码

建议目录：

```text
features/
  import/
  overview/
  command-center/
  pl/
  traffic/
  inventory/
  refund/
  review/
  keyword/
  simulator/
  ai/
shared/
  ui/
  data/
  formatting/
  metrics/
```

- `DetailAnalysisModal` 拆为数据模型、列定义、筛选、表格、复制/导出、AI 诊断等独立模块。
- 业务指标公式集中到 `shared/metrics`，UI 不自行重复计算。
- 数据解析使用显式 schema 和类型化错误，不再依赖大面积 `any`。
- 删除未使用依赖；例如当前 DB 已使用原生 IndexedDB，需要确认 `idb` 是否仍有必要。

## 6. 关键用户路径验收

| 路径 | 必测场景 | 通过标准 |
|---|---|---|
| 首次导入 | 正常文件、缺表、错表头、空文件、大文件 | 错误可定位；旧数据不受影响；成功后口径可见 |
| 更新数据 | 只更新部分可选表、解析中失败、写库失败 | 明确覆盖范围；可撤销；动作数据不丢失 |
| 月/周切换 | 无周表、有周表、最近周不在当前自然周 | 开关始终可访问；默认定位最近有效周期 |
| 筛选 | 空选、全不选、多层维度、恢复快照 | 结果数、有效筛选和口径始终一致 |
| 异常到动作 | 创建、改负责人、逾期、完成、忽略、复盘 | 状态持久化；更新后可对比效果 |
| 子表 | 排序、筛选、趋势、复制、导出、大数据滚动 | 总计与可见范围一致；操作可撤销/反馈明确 |
| AI | 未配置、超时、取消、敏感字段、模型错误 | 不泄露 Key；范围可预览；错误可恢复 |
| 可访问性 | 键盘、200% 缩放、屏幕阅读器、高对比度 | 无焦点丢失；语义完整；不只依赖颜色 |

## 7. 测试与发布策略

### 7.1 自动化测试金字塔

- **单元测试**：日期周期、筛选、聚合、目标分摊、pacing、规则阈值、金额/百分比格式。
- **契约测试**：各类 Excel/CSV 列别名、空值、日期格式、图片 ID、跨表关联。
- **组件测试**：指标卡、异常条目、筛选器、统一 Dialog、导入摘要。
- **E2E**：导入 → 筛选 → 下钻 → 创建动作 → 更新数据 → 复盘。
- **视觉回归**：亮/暗主题 × 1024/1280/1366/1440/1920。
- **性能测试**：1 MB、10 MB、50 MB 样本；记录解析时长、主线程长任务、内存峰值。

### 7.2 发布方式

- 先对 2–3 名真实运营灰度，收集任务完成时间和口径问题。
- 数据层改造必须支持旧 IndexedDB 版本迁移和回滚。
- P0/P1 分开发布；不要把数据底座改造和大规模视觉重构塞进同一个不可回退版本。
- 每次发布附带“指标口径是否变化、缓存是否迁移、是否需要重新导入”的说明。

## 8. 成功指标

| 维度 | 指标 | 目标 |
|---|---|---|
| 核心效率 | 导入后定位 Top 3 问题的中位时长 | ≤ 3 分钟 |
| 导入可靠性 | 有效模板导入成功率 | ≥ 98% |
| 数据安全 | 导入失败导致旧数据变化 | 0 次 |
| 指标正确性 | 金标数据核心指标差异 | 0 |
| 决策闭环 | 高优问题生成负责人动作的比例 | ≥ 70% |
| 复盘闭环 | 已完成动作中有结果回填的比例 | ≥ 80% |
| 性能 | 初始业务 JS gzip | < 300 KB |
| 性能 | 已缓存数据恢复可操作时间 | < 1.5s |
| 稳定性 | 无未处理错误的会话比例 | ≥ 99.5% |
| 可访问性 | 关键路径键盘完成率 | 100% |
| 工程质量 | 主分支 CI 通过率 | 100% |

指标上线前先记录一周基线；涉及业务行为的目标可根据真实基线二次校准。

## 9. 建议的第一轮 Sprint（10 个工作日）

### Sprint 目标

把产品从“能分析”提升到“导入不丢、指标可验、常见屏幕可用、生产不依赖 CDN”。

### Backlog

1. 建立脱敏金标样本与 20 个核心指标对账表。
2. 修复 `dataLoader.ts:419` 类型错误，增加 `typecheck` 脚本。
3. 重构导入为全部解析成功后再提交；增加失败回滚。
4. DB API 不再吞错，写入全部等待完成并反馈。
5. 增加导入流程与核心指标的首批单元/E2E 测试。
6. 移除客户端生产 Key 注入，增加 AI 发送范围确认。
7. 本地化 Tailwind 与字体，删除外部运行时依赖和 import map。
8. 修复 1024–1280 布局：可折叠侧栏、模式开关常驻、雷达自适应。
9. 顶栏增加周期、数据截至日期和有效筛选摘要。
10. 建立 CI 门禁和 bundle budget。

### Sprint Done 定义

- 所有 P0 用例有自动化或明确的手工验收记录。
- 断网可启动并完整渲染基础看板。
- 1024–1920 的核心流程无功能隐藏和非预期横向溢出。
- 无 TypeScript 错误；生产构建、测试和 bundle 检查全绿。
- 错误导入不会改变任何现有数据。

## 10. 暂不建议立即做的事项

- 在数据可信和动作闭环完成前继续增加新报表。
- 为手机端完整复刻所有宽表和复杂子表。
- 在没有数据范围控制、密钥治理和成本限制前扩大 AI 使用场景。
- 先做全面品牌视觉翻新，再处理导入、响应式和性能问题。
- 过早引入账号、权限和多人后台；先验证单机工作流的真实使用频率和闭环价值。

## 11. 最终建议

优先顺序应保持为：

> **数据不丢与指标正确 → AI/密钥安全 → 常见屏幕可用 → 决策闭环 → 性能与代码拆分 → 多人协作与新功能。**

如果只能做一个版本，就执行上面的第一轮 Sprint；它会比再增加一个分析模块更直接地提升产品可信度、使用频率和可持续迭代能力。
